# Nextcloud + MariaDB for External Storage S3 AP verification
# Usage: docker compose up -d
# Access: http://localhost:8080
#
# This is a LOCAL TEST SETUP — not for production.
# See docs/nextcloud-external-storage-s3ap.md for production guidance.

services:
  nextcloud:
    image: nextcloud:34
    container_name: nextcloud-s3ap-test
    ports:
      - "8080:80"
    environment:
      - MYSQL_HOST=db
      - MYSQL_DATABASE=nextcloud
      - MYSQL_USER=nextcloud
      - MYSQL_PASSWORD=nextcloud
      - NEXTCLOUD_ADMIN_USER=admin
      - NEXTCLOUD_ADMIN_PASSWORD=admin123
    volumes:
      - nextcloud_data:/var/www/html
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  db:
    # Pinned to 11.8 deliberately. Nextcloud 34 supports MariaDB 10.6 / 10.11 /
    # 11.4 / 11.8 — MariaDB 12 is not on that list, so do not accept a major
    # bump here without checking the Nextcloud system requirements first.
    # https://docs.nextcloud.com/server/stable/admin_manual/installation/system_requirements.html
    image: mariadb:11.8
    container_name: nextcloud-db
    environment:
      - MYSQL_ROOT_PASSWORD=rootpass
      - MYSQL_DATABASE=nextcloud
      - MYSQL_USER=nextcloud
      - MYSQL_PASSWORD=nextcloud
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mariadb-admin", "ping", "-h", "localhost", "-u", "root", "-prootpass"]
      interval: 5s
      timeout: 3s
      retries: 10
    restart: unless-stopped

volumes:
  nextcloud_data:
  db_data:
